/* Operadores de comparacion */

// == igual
// === igual igual
// != diferente
// !== diferente diferente
// < menor que
// <= menor o igual que
// > mayor que
// >= mayor o igual que

/* Operadores Logicos */
// && AND
// || OR
// ! NOT

/* Estructuras de control */

if (condicion) {
    // ejecutar este bloque
}
if (condition) {
    
} else {
    
}

// ternaria

condicion ? valor1 : valor2;